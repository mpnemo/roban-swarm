# Roban Swarm — Helicopter companion environment file
# Installed to /etc/roban-swarm/heli.env
#
# This file contains all per-heli configuration.
# Secrets (passphrase, NTRIP creds) must be filled in after install.
# This example file is safe to commit; the actual heli.env is in .gitignore.

# --- Identity ---
HELI_ID=01

# --- Network ---
BASE_IP=192.168.50.1
WIFI_SSID=RTK-FIELD
# WIFI_PASSPHRASE is set separately in NetworkManager profile
# Do NOT store it here if this file might be committed

# --- NTRIP (RTCM corrections) ---
NTRIP_PORT=2101
NTRIP_MOUNT=BASE
NTRIP_USER=admin
NTRIP_PASS=REPLACE_WITH_NTRIP_PASSWORD

# --- Serial ports ---
# Use /dev/serial/by-id/ paths for stability.
# Run companion/tools/detect_ports.sh to discover available ports.
FC_SERIAL=/dev/serial/by-id/REPLACE_WITH_FC_SERIAL
FC_BAUD=921600
GNSS_RTCM_SERIAL=/dev/serial/by-id/REPLACE_WITH_GNSS_SERIAL
GNSS_RTCM_BAUD=115200

# --- MAVLink ---
# Derived from HELI_ID:
#   UDP_PORT (telemetry outbound) = 14559 + HELI_ID
#   CMD_PORT (command inbound)    = 14659 + HELI_ID
UDP_PORT=14560
CMD_PORT=14660
