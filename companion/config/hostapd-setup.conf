# Roban Swarm — hostapd config for provisioning AP mode
# Installed to /etc/roban-swarm/hostapd-setup.conf

interface=wlan0
driver=nl80211
ssid=RobanHeli-SETUP
hw_mode=g
channel=6
wmm_enabled=0
auth_algs=1
wpa=2
wpa_passphrase=robansetup
wpa_key_mgmt=WPA-PSK
rsn_pairwise=CCMP
